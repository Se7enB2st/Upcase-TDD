<%= link_to "Add a new todo", new_todo_path %>

<ul class="todos">
  <%= @todos.each do |todo| %>
  <li>
    <%=todo.title %>
    <%=button_to 'Completed!',todo_completion_path(todo) %>
  </li>

  <% end %>
</ul>
